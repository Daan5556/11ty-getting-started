---
title: Dishes
layout: layout.liquid
---
{% include "dish-stats.liquid" %}
<ul>
  {%- for post in collections.post -%}
    <li>
      {% if post.url%}
      <a href="{{ post.url }}">
        {{ post.data.title }}
      </a>
    {% else %}
        {{ post.data.title }}
    {% endif %}
    </li>
  {%- endfor -%}
</ul>
